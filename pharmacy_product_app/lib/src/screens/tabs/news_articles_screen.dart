import 'package:flutter/material.dart';

class NewsArticlesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('اخبار و مقالات')),
      body: Center(child: Text('News & Articles Screen')),
    );
  }
}
