import 'package:flutter/material.dart';

class HerbalScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('گیاهی')),
      body: Center(child: Text('Herbal Products Screen')),
    );
  }
}
