import 'package:flutter/material.dart';

class SupplementsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('مکمل ها')),
      body: Center(child: Text('Supplements Screen')),
    );
  }
}
